读取授权案例、弹窗协议和两个页面，确认案例主键、来源模式、state、确认端点、超时与预期终态。
为每个案例建立独立browser context，从商户页按钮打开真实popup并保留对应窗口对象。
在随机回环端口分别提供商户页、可信授权页和非可信授权页，使消息来源由浏览器origin决定。
观察popup发送的postMessage，依次检查event.origin、event.source、消息类型、state和结果枚举。
让合法approved消息调用本地确认端点一次，让合法declined消息直接建立拒绝终态。
将来源或state不符的消息写入拒绝台账，并让仍在等待的页面由超时路径收口。
轮询popup.closed识别用户关窗，在任一终态建立后拒绝重复消息并阻止重复确认。
按案例输出终态矩阵，按消息到达次序输出协议台账，按实际POST顺序输出确认请求。
保存商户页终态截图，再从三份业务报告和截图集合生成对账摘要。
